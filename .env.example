# Copy to .env and adjust if you're not using the docker-compose.yml defaults.
# database.py falls back to this exact value if DATABASE_URL isn't set.
DATABASE_URL=postgresql+psycopg2://urlshort:urlshort@localhost:5432/urlshortener
